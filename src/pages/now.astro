---
import { SITE_DESCRIPTION, SITE_TITLE } from '../consts';
import SiteShell from '../layouts/SiteShell.astro';
---

<SiteShell title={`${SITE_TITLE} | Now`} description={SITE_DESCRIPTION}>
	<section class="page">
		<h1>Now</h1>
		<p class="intro">
			A quick snapshot of what I am actively building, learning, and improving right now.
		</p>

		<div class="grid">
			<section class="card">
				<h2>Current Focus</h2>
				<ul>
					<li>Platform reliability through simpler deployment and rollback paths.</li>
					<li>AI and agent-building skills applied to practical engineering workflows.</li>
					<li>Reducing operational noise with cleaner alerts, runbooks, and ownership.</li>
				</ul>
			</section>

			<section class="card">
				<h2>In Progress</h2>
				<ul>
					<li>Tightening GitHub Actions workflows for faster, safer releases.</li>
					<li>Turning repeated tasks into reusable checklists and templates.</li>
					<li>Publishing shorter notes from real experiments each week.</li>
				</ul>
			</section>
		</div>

		<section class="card">
			<h2>Recent Changes</h2>
			<p>
				I launched this blog as a static site on GitHub Pages with ticket-based planning in Linear,
				and I am iterating content and design in small, reviewable passes.
			</p>
		</section>
	</section>
</SiteShell>

<style>
	.page {
		max-width: 880px;
	}

	.intro {
		max-width: 62ch;
		margin-bottom: var(--space-6);
	}

	.grid {
		display: grid;
		grid-template-columns: repeat(2, minmax(0, 1fr));
		gap: var(--space-4);
		margin-bottom: var(--space-4);
	}

	.card {
		padding: var(--space-5);
		border: 1px solid var(--line-subtle);
		border-radius: var(--radius-md);
		background: color-mix(in srgb, var(--panel-bg), white 12%);
	}

	.card h2 {
		font-size: 1.1rem;
		margin-bottom: var(--space-3);
	}

	.card ul {
		margin: 0;
		padding-left: 1.1rem;
	}

	.card li {
		margin-bottom: 0.45rem;
	}

	.card p:last-child {
		margin-bottom: 0;
	}

	@media (max-width: 800px) {
		.grid {
			grid-template-columns: 1fr;
		}
	}
</style>
