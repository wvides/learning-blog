---
import { withBase } from '../lib/paths';
const today = new Date();
---

<footer>
	<p>© {today.getFullYear()} Walter Vides.</p>
	<p>Notes from experiments, systems, and things learned.</p>
	<p><a href={withBase('/changelog')}>Changelog</a></p>
</footer>

<style>
	footer {
		padding: 1.25rem 1.25rem 1.75rem;
		border-top: 1px solid var(--line-subtle);
		background: color-mix(in srgb, var(--panel-bg), white 30%);
		text-align: center;
	}

	:root[data-theme="dark"] footer {
		background: color-mix(in srgb, var(--panel-bg), #0c3c45 40%);
	}

	footer p {
		margin: 0;
		font-size: 0.8rem;
		color: var(--text-muted);
	}

	footer p + p {
		margin-top: 0.25rem;
	}

	footer a {
		color: var(--text-muted);
		text-underline-offset: 0.18rem;
	}

	footer a:hover {
		color: var(--text-strong);
	}
</style>
